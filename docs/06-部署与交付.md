# 部署与交付

## 一、交付物清单

| 内容 | 位置 | 说明 |
|---|---|---|
| 后端源码 | apps/server | NestJS + Drizzle，含 15 个 AI 工具与完整电商接口 |
| 管理后台源码 | apps/admin | Vue3 + Element Plus + ECharts，8 个页面 |
| 小程序源码 | apps/miniapp | 原生微信小程序，14 个页面 |
| 共享类型 | packages/shared | 前后端共用的枚举与 Zod 校验 |
| 数据库迁移 | apps/server/drizzle | 29 张表的建表 SQL |
| 演示数据 | apps/server/src/db/seed.ts | 29 商品 + 用户 + 订单 + 评价 + 8 条售后政策 + 演示 AI 会话 |
| 自动化测试 | tests/ | 验收 34 项 + 小程序 22 项 |
| 文档 | docs/ | 架构 / 数据库 / AI 设计 / 后台 / 小程序 / 部署 / 演示脚本 |

## 二、环境要求

| 依赖 | 版本 | 是否必须 |
|---|---|---|
| Node.js | >= 20（开发机实测 26） | 必须 |
| pnpm | >= 10（实测 11.5） | 必须 |
| Docker | 任意 | **可选**，只有切真实 PostgreSQL 时才需要 |
| 微信开发者工具 | 最新稳定版 | 只有调小程序时需要 |
| 大模型 API Key | 任意 OpenAI 兼容服务 | **可选**，不配也能完整演示 |

## 三、快速开始（零安装路径，推荐）

本地开发**不需要 Docker，也不需要安装 PostgreSQL**：默认使用 PGlite（编译成 WASM 的 PostgreSQL 18，内置 pgvector 0.8），数据库直接跑在 Node 进程里。

```bash
pnpm install
cp .env.example .env
pnpm setup        # = db:push + db:seed + demo:prepare
pnpm dev:server   # http://localhost:3100/api   （接口文档 /api/docs）
pnpm dev:admin    # http://localhost:5173       （admin / admin123）
```

小程序：微信开发者工具导入 `apps/miniapp` → 详情 → 本地设置 → **勾选「不校验合法域名」**。不需要真实 AppID。

## 四、切换到真实 PostgreSQL

```bash
docker compose up -d postgres redis
# 修改 .env：
#   DB_DRIVER=postgres
#   DATABASE_URL=postgres://campus:campus@localhost:5432/campus_mall
pnpm db:push && pnpm db:seed && pnpm demo:prepare
```

同一份 Drizzle schema、同一套迁移 SQL 同时驱动 PGlite 与 PostgreSQL，业务代码零改动。
注意 PGlite 的数据落在 `apps/server/.data/pg`，切库后需要重新 seed。

## 五、环境变量清单

| 变量 | 默认值 | 说明 |
|---|---|---|
| NODE_ENV | development | |
| PORT | 3100 | 后端端口 |
| API_PREFIX | /api | 全局前缀，改这里前端 baseURL 要同步 |
| DB_DRIVER | pglite | `pglite` 或 `postgres` |
| PGLITE_DATA_DIR | .data/pg | PGlite 数据目录（相对 apps/server） |
| DATABASE_URL | — | `DB_DRIVER=postgres` 时必填 |
| JWT_SECRET | dev-secret-change-me | **上线必须改** |
| JWT_EXPIRES_IN | 7d | |
| LLM_BASE_URL | https://api.deepseek.com/v1 | 任意 OpenAI 兼容地址 |
| LLM_API_KEY | 空 | **留空即自动降级为本地规则引擎** |
| LLM_MODEL | deepseek-chat | 对话模型 |
| LLM_VISION_MODEL | 空 | 配了才能「拍照找同款」，否则走降级提示 |
| LLM_EMBEDDING_MODEL | 空 | 配了才能语义检索，需再跑 `pnpm ai:embed` |
| LLM_EMBEDDING_DIM | 1024 | 改这个要重新生成迁移（DDL 里写死了 vector(1024)） |
| LLM_TIMEOUT_MS | 60000 | |
| WX_APPID / WX_SECRET | 空 | 留空走开发模式：code 直接映射为 openid，无需真实小程序 |

## 六、接入真实大模型

### DeepSeek（推荐，中文与工具调用都稳）

```
LLM_BASE_URL=https://api.deepseek.com/v1
LLM_API_KEY=sk-xxx
LLM_MODEL=deepseek-chat
```

### 通义千问（含视觉与向量，一个 Key 全搞定）

```
LLM_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
LLM_API_KEY=sk-xxx
LLM_MODEL=qwen-plus
LLM_VISION_MODEL=qwen-vl-plus
LLM_EMBEDDING_MODEL=text-embedding-v3
```

### 本地 Ollama（完全离线）

```
LLM_BASE_URL=http://localhost:11434/v1
LLM_API_KEY=ollama
LLM_MODEL=qwen2.5:14b
```

改完重启后端即可，`GET /api/ai/status` 会显示当前 provider、模型与是否降级。
配了 embedding 模型后执行 `pnpm ai:embed` 给商品与知识库补向量，检索自动从关键词升级为语义。

## 七、生产部署

### 后端

```bash
pnpm --filter @campus/server build
NODE_ENV=production node apps/server/dist/main.js
```

建议用 pm2 或 systemd 托管；上传的图片在 `apps/server/uploads`，记得挂持久化卷或用对象存储。

### 管理后台

```bash
pnpm --filter @campus/admin build   # 产物在 apps/admin/dist
```

nginx 示例：

```nginx
server {
  listen 80;
  root /var/www/campus-admin;      # apps/admin/dist
  location / { try_files $uri $uri/ /index.html; }
  location /api/ { proxy_pass http://127.0.0.1:3100/api/; }
  location /uploads/ { proxy_pass http://127.0.0.1:3100/uploads/; }
}
```

### 小程序

1. 改 `apps/miniapp/config.js` 的 `BASE_URL` 为正式域名
2. 小程序后台配置 request 合法域名与 uploadFile 域名
3. 配置 WX_APPID / WX_SECRET 后，登录会自动切到真实 code2session
4. 开发者工具上传 → 提交审核

## 八、常见问题

| 问题 | 原因与处理 |
|---|---|
| 小程序请求全失败 | 没勾「不校验合法域名」，或后端没启动 |
| AI 回复是模板话术 | 正常：没配 `LLM_API_KEY` 时的降级行为，`/api/ai/status` 会显示 mock |
| 拍照找同款提示「看不了图」 | 没配 `LLM_VISION_MODEL`；这是刻意设计，会引导用户描述或扫码 |
| 搜索结果不语义 | 没配 `LLM_EMBEDDING_MODEL`，或配了但没跑 `pnpm ai:embed` |
| 想清空数据重来 | `pnpm db:reset`（会重建 29 张表并重新灌演示数据） |
| 端口被占用 | 改 `.env` 的 PORT，同时改 `apps/admin/vite.config.ts` 的代理目标 |
| 迁移报 vector 相关错误 | 建表必须先 `CREATE EXTENSION vector`，用 `pnpm db:push` 而不是 drizzle-kit push |

## 九、二次开发：加一个新的 AI 能力

三步，不需要改 Agent 与前端：

1. 在 `apps/server/src/modules/ai/tools/` 写一个类，实现 `AiTool`（name / description / schema / run）
   - 只读工具直接 `run`；写操作加 `write: true` 并实现 `confirmSummary`，框架会自动走二次确认
   - 想让工具只在某些场景出现，填 `scenes: ['merchant']`
2. 把类注册进 `ToolRegistry` 构造函数
3. 重启后端，`GET /api/ai/status` 的 tools 列表里就会出现它，模型也能直接调用

前端零改动：工具调用过程会通过 `tool_start` / `tool_result` 事件自动展示。

## 十、验收方式

```bash
pnpm test:acceptance   # 34 项：后端 + AI + 管理端接口全链路
pnpm test:miniapp      # 22 项：小程序 API 层真实打后端 + SSE 跨 chunk 解码
pnpm test              # 两个一起跑
```

验收脚本不依赖浏览器与微信开发者工具，纯 HTTP + Node 桩，适合交付前自查或买家自测。
