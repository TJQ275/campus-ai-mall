# 原生微信小程序说明

## 一、运行

1. 先启动后端：`pnpm dev:server`（默认 http://localhost:3100）
2. 微信开发者工具 → 导入项目 → 选择 `apps/miniapp` 目录
3. **详情 → 本地设置 → 勾选「不校验合法域名」**（本地开发必须，否则请求被拦）
4. 后端地址在 `apps/miniapp/config.js` 里改，上线前换成正式域名并在小程序后台配置 request 合法域名

AppID 用测试号即可：`project.config.json` 里填的是 `touristappid`。

## 二、登录说明

**不需要真实 AppID 也能跑通全流程**：后端未配置 `WX_APPID` 时走开发模式，
`wx.login` 拿到的 code 直接映射成 openid（`dev-<code>`），自动建号并返回 token。
配置了 `WX_APPID` / `WX_SECRET` 后自动切换为真实的 `jscode2session` 换取 openid，代码不用改。

## 三、页面

| 页面 | 路径 | 说明 |
|---|---|---|
| 首页 | pages/index | Banner、分类入口、AI 助手入口（带快捷提问）、带推荐理由的个性化推荐、热销 |
| 分类 | pages/category | 零食 / 二手书切换、分类横向筛选、排序、上拉加载 |
| 搜索 | pages/search | 关键词搜索 + **扫码找书**（扫 ISBN 条码精确匹配，不依赖模型） |
| 商品详情 | pages/product/detail | 图片、价格、SKU 选择、**AI 评论摘要**、评价列表、底部「问 AI」带页面上下文 |
| 购物车 | pages/cart | 勾选、改数量、删除、金额与配送费、去结算 |
| 确认订单 | pages/order/confirm | 地址选择、支付方式（余额 / 微信 / 支付宝模拟）、备注、提交并支付 |
| 订单列表 | pages/order/list | 按状态分页签，支付 / 取消 / 确认收货 / 申请售后 |
| 订单详情 | pages/order/detail | 明细、金额、地址快照；AI 促成的订单会标注 |
| 申请售后 | pages/after-sale/apply | 仅退款 / 退货退款、原因、说明 |
| 售后记录 | pages/after-sale/list | 进度、处理意见、撤销；AI 代提交的会标注 |
| 收货地址 | pages/address/list、edit | 增删改、设为默认 |
| 我的 | pages/mine | 余额与充值、订单状态计数、入口聚合、AI 模式提示 |
| **AI 助手** | pages/ai/chat | 见下节 |

## 四、AI 助手页（核心）

### 流式实现

小程序没有 `EventSource`，只能用 `wx.request({ enableChunked: true })` + `task.onChunkReceived`，
而且回调拿到的是 `ArrayBuffer`。`utils/sse.js` 做了两件事：

1. **增量 UTF-8 解码**：一个汉字可能被切在两个 chunk 里，必须保留不完整字节等下一个 chunk 再拼
2. **SSE 切帧**：一帧也可能跨 chunk，用 `\n\n` 做边界并缓存半截数据

这两件事都有单测覆盖（`tests/miniapp-sse.test.cjs`，用 Node 桩替换 wx API，故意在汉字中间切断数据流）。

基础库过低不支持分块时，自动回退到 `POST /api/ai/chat/sync` 一次性拿结果，界面功能不受影响。

### 界面元素

| 元素 | 说明 |
|---|---|
| 工具过程 | 每次工具调用显示一个黄色小条，完成后替换为结果摘要（例如「找到 5 件商品，展示前 5 件」） |
| 商品卡片 | 由后端 `cards` 事件推送，点击进详情 |
| **写操作确认** | AI 想加购 / 提交售后时出现「取消 / 确认执行」按钮，点确认才真正执行 |
| 消息元信息 | 显示模型名与耗时，降级模式下标注「本地演示模式」 |
| 场景切换 | 导购 / 客服两个场景，客服场景下助手会先查知识库并引用条款 |
| 扫码找书 | 直接调用扫码，把 ISBN 作为一句话发给助手 |

### 页面上下文

商品详情页点「问 AI」会先把 `{ page: 'product', productId }` 写入 storage，
聊天页读取后随请求带上，因此用户说「这个辣不辣」时助手知道「这个」指谁。

## 五、自动化测试

```bash
pnpm test:miniapp
```

- `miniapp-sse.test.cjs`：SSE 客户端事件序列与跨 chunk 汉字解码
- `miniapp-api.test.cjs`：用 Node 桩替换 `wx.request` 真实打后端，跑通「登录 → 浏览 → 加购 → 下单 → 支付 → 售后 → AI 对话与确认」共 22 项断言

## 六、尚未做的部分

- **tabBar 图标**：当前是纯文字 tabBar（微信允许不配图标）。要加图标就把 png 放进 `apps/miniapp/assets/` 并补 `iconPath` / `selectedIconPath`
- 微信支付：当前是模拟支付（余额 / 微信 / 支付宝都只改状态），接真实支付需要商户号与证书
- 订阅消息、分享卡片、分包加载
